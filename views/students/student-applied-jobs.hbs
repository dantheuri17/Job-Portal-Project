<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Applied Jobs</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f9fa;
        }

        .header {
            background-color: #343a40;
            color: #ffffff;
            text-align: center;
            padding: 1rem;
        }

        .container {
            max-width: 800px;
            margin: 2rem auto;
            padding: 2rem;
            background-color: #ffffff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }

        .job {
            padding: 1rem;
            border-bottom: 1px solid #e0e0e0;
        }

        .job-title {
            font-size: 1.2rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .job-info {
            color: #777777;
            margin-bottom: 0.25rem;
        }

        .no-jobs {
            text-align: center;
            color: #777777;
            margin-top: 2rem;
        }

        .job-status {
            font-weight: bold;
            padding: 0.2rem 0.5rem;
            border-radius: 4px;
            text-transform: uppercase;
        }

        .status-Accepted {
            color: #28a745;
            background-color: #d4edda;
        }

        .status-Rejected {
            color: #dc3545;
            background-color: #f8d7da;
        }

        .status-Pending {
            color: #ffc107;
            background-color: #fff3cd;
        }

        /* Sidebar Styles */
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 250px;
            height: 100vh;
            background-color: #333;
            padding: 20px;
            border-radius: 0 5px 5px 0;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            z-index: 1;
        }

        .sidebar a {
            display: block;
            margin-bottom: 10px;
            padding: 10px;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.2s;
        }

        .sidebar a:hover {
            background-color: #e1e1e1;
        }
    </style>
</head>

<body>
    <div class="header">
        <h1>Jobs Applied by {{ student.name }}</h1>
    </div>
    <div class="sidebar">
        <a href="/student/profile">Student Profile</a>
        <a href="/student/settings">Settings</a>
        <a href="/student/saved-posts">Saved Posts</a>
        <a href="/student/dashboard">Dashboard</a>
        <a href="/logout">Logout</a>
    </div>
    <div class="container">
        {{#if allAppliedJobs.length}}
        <ul>
            {{#each allAppliedJobs}}
            <li class="job">
                <div class="job-title">{{ jobTitle }}</div>
                <div class="job-info">Company: {{ company }}</div>
                <div class="job-info">Location: {{ employerLocation }}</div>
                <div class="job-info">Status:
                    <span class="job-status status-{{ status }}">{{ status }}</span>
                </div> 
            </li>
            {{/each}}
        </ul>
        {{else}}
        <p class="no-jobs">No jobs applied yet.</p>
        {{/if}}
    </div>
</body>

</html>