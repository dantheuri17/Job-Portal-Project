<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <div class="sidebar">
                    <a href="/student/profile" class="d-block">Student Profile</a>
                    <a href="/student/settings" class="d-block">Settings</a>
                    <a href="/student/saved-posts" class="d-block">Saved Posts</a>
                    <a href="/student/applied-jobs" class="d-block">Applied Jobs</a>
                    <a href="/logout" class="d-block">Logout</a>
                </div>
            </div>
            <div class="col-md-9">
                <h2 class="text-center mt-4 mb-4">Welcome to your profile</h2>
                <div class="row">
                    <div class="col-md-6">
                        <img src="{{ student.profilePicture }}" alt="Profile Picture"
                            class="img-thumbnail profile-picture">
                    </div>
                    <div class="col-md-6">
                        <div class="student-info">
                            <h3>{{student.firstName}} {{student.lastName}}</h3>
                            <p><strong>Major:</strong> {{ student.major }}</p>
                            <p><strong>Email:</strong> {{ student.email }}</p>
                            <p><strong>Industry:</strong> {{ student.industry }}</p>
                            <p><strong>Location:</strong> {{ student.location }}</p>
                            {{#if student.resume}}
                            <a href="/student/resume" class="btn btn-primary mt-3">View Resume</a>
                            {{/if}}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>