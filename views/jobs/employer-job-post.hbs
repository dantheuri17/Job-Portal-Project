<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <title>Job Post Details</title>
    <style>
        .sidebar {
            background-color: #f8f9fa;
            height: 100vh;
            width: 15%; 
            /* Set sidebar to full viewport height */
            padding: 20px;
        }

        .card {
            margin-top: 20px;
        }
        .sidebar > ul > li {
            list-style: none;
        }
       .sidebar ul > li > a {
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-3 sidebar">
                <ul>
                    <li><a href="/employer/dashboard">Dashboard</a></li>
                    <li><a href="/employer/profile">Profile</a></li>
                    <li><a href="/employer/postJob">Post Job</a></li>
                    <li><a href="/employer/settings">Settings</a></li>
                    <li><a href="/employer/job-applications">Application Dashboard</a></li>
                    <li><a href="/logout">Logout</a></li>
                </ul>
            </div>

            <!-- Content -->
            <div class="col-md-9">
                <div class="container mt-5">
                    <h1>Job Post Details</h1>

                    <!-- Display flash message if it exists -->
                    {{#if flash.success}}
                    <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
                        {{flash.success}}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                    {{/if}}

                    {{#if flash.error}}
                    <div class="alert alert-danger alert-dismissible fade show mt-3" role="alert">
                        {{flash.error}}
                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                    {{/if}}

                    {{#if jobPost}}
                    <div class="card mt-3">
                        <div class="card-body">
                            <h5 class="card-title">{{jobPost.jobTitle}}</h5>
                            <p class="card-text"><strong>Company:</strong> {{jobPost.company}}</p>
                            <p class="card-text"><strong>Location:</strong> {{jobPost.employerLocation}}</p>
                            <p class="card-text"><strong>Job Type:</strong> {{jobPost.jobType}}</p>
                            {{#if jobPost.closingDate}}
                            <p class="card-text"><strong>Closing Date:</strong> {{jobPost.closingDate}}</p>
                            {{/if}}
                            <p class="card-text"><strong>Job Description:</strong> {{{jobPost.jobDescription}}}</p>
                    
                            <!-- Add "Edit" and "Delete" buttons with Bootstrap classes -->
                            <a href="/employer/edit-job/{{jobPost._id}}" class="btn btn-primary me-2">Edit Job</a>
                            <a href="/employer/delete-post/{{jobPost._id}}" class="btn btn-danger">Delete Job</a>
                        </div>
                    </div>
                    {{else}}
                    <p class="mt-3">Job details not found.</p>
                    {{/if}}
                </div>
            </div>
        </div>
    </div>
</body>

</html>