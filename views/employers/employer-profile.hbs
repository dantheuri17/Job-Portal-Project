<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employer Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <style>
        .profile-logo {
            max-width: 100%;
            height: auto;
            margin-bottom: 20px;
        }

        .employer-info {
            margin-bottom: 30px;
        }

        .employer-info h3 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .employer-info p {
            font-size: 18px;
            margin-bottom: 10px;
        }

        .welcome-heading {
            text-align: center;
            margin-top: 40px;
            margin-bottom: 40px;
            font-size: 30px;
            font-weight: bold;
        }

        .sidebar {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 5px;
        }

        .sidebar h4 {
            font-size: 20px;
            margin-bottom: 10px;
        }

        .sidebar ul {
            list-style-type: none;
            padding: 0;
            margin: 0;
        }

        .sidebar li {
            margin-bottom: 10px;
        }

        .sidebar a {
            color: #000;
            text-decoration: none;
        }

        .sidebar a:hover {
            color: #000;
            text-decoration: underline;
        }

        .profile-logo {
            max-width: 100%;
            /* Adjust this value to control the maximum width */
            height: auto;
            /* Automatically adjusts the height while maintaining aspect ratio */
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <div class="sidebar">
                    <h4>Menu</h4>
                    <ul>
                        <li><a href="employer/dashboard">Dashboard</a></li>
                        <li><a href="/employer/settings">Settings</a></li>
                        <li><a href="/employer/application-dashboard">Application Dashboard</a></li>
                        <li><a href="/logout">Logout</a></li>
                    </ul>
                </div>
            </div>
            <div class="col-md-9">
                <h2 class="welcome-heading">Welcome to your profile</h2>
                <div class="row">
                    <div class="col-md-6">
                        <img class="profile-logo" src="data:image/png;base64,{{employer.logo.logoImageBinary.base64}}"
                            alt="Employer Logo">
                    </div>
                    <div class="col-md-6">
                        <div class="employer-info">
                            <h3>{{ employer.companyName }}</h3>
                            <p><strong>Industry:</strong> {{ employer.industry }}</p>
                            <p><strong>No. of Employees:</strong> {{ employer.noOfEmployees }}</p>
                            <p><strong>Location:</strong> {{ employer.employerLocation }}</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>